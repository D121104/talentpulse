# Optional root environment for backend/environment/docker-compose.yml interpolation.
# Copy this file to .env for local Compose use; these values are disposable and
# local-only. Never use them, or any values in this file, in production.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123
POSTGRES_DB=recruitment_db
REDIS_PASSWORD=redis123
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin
